# ブロック崩しゲーム設計書

## 1. 概要設計

### 1.1 システム概要
- **プロジェクト名**: ブロック崩しゲーム
- **開発言語**: JavaScript (p5.js)
- **対象デバイス**: PC、スマートフォン、タブレット
- **実行環境**: Webブラウザ

### 1.2 アーキテクチャ
```
┌─────────────────┐
│   UI Layer      │ ← index.html, style.css
├─────────────────┤
│  Game Logic     │ ← sketch.js (p5.js メイン関数のみ)
│                 │   js/systems/ (ゲーム管理)
│                 │   js/classes/ (オブジェクト定義)
│                 │   js/utils/ (ユーティリティ)
├─────────────────┤
│ Configuration   │ ← config.js (設定・定数)
├─────────────────┤
│ Resource Layer  │ ← img/, mp3/
└─────────────────┘
```

## 2. 詳細設計

### 2.1 ゲーム状態管理

#### 2.1.1 ゲームステート
```javascript
const GAME_STATE = {
    OPENING: 'opening',
    PLAYING: 'playing',
    PAUSED: 'paused',
    GAME_OVER: 'gameOver',
    LEVEL_CLEAR: 'levelClear'
};
```

#### 2.1.2 状態遷移図
```
[opening] --click--> [playing] --pause--> [paused]
    ↑                    ↓                   ↓
    |              [level_clear]        [playing]
    |                    ↓                   
    └---- [game_over] <--┴-- all lives lost
```

### 2.2 データ構造設計

#### 2.2.1 ゲーム設定
```javascript
const gameConfig = {
    canvas: {
        width: 800,
        height: 600,
        scaleFactor: 1.0
    },
    player: {
        lives: 3,
        maxLives: 3,
        score: 0,
        level: 1
    },
    paddle: {
        width: 80,
        height: 15,
        expandedWidth: 120,
        expandDuration: 20000, // ms
        slowDuration: 5000     // ms
    }
};
```

#### 2.2.2 ブロック配置
```javascript
const blockLayout = {
    rows: 4,
    cols: 5,
    width: 60,
    height: 20,
    spacing: 10,
    colors: ['#FF6B6B', '#4ECDC4', '#45B7D1', '#96CEB4', '#FFEAA7'],
    specialBlockRatio: 0.3  // 30%がアイテムブロック
};
```

#### 2.2.3 アイテムシステム
```javascript
const itemConfig = {
    types: {
        LIFE_UP: { id: 'hp', image: 'img/hp.png', effect: 'addLife' },
        PADDLE_EXPAND: { id: 'shield', image: 'img/shield.png', effect: 'expandPaddle' },
        BALL_MULTIPLY: { id: 'ball', image: 'img/ball.png', effect: 'multiplyBall' },
        SLOW_PENALTY: { id: 'skull', image: 'img/skull.png', effect: 'slowPaddle' }
    },
    spawnRates: {
        level1: { good: [5,7], penalty: [2,3] },
        levelUp: { goodDecrease: 1, penaltyIncrease: 1, maxPenalty: 6 }
    }
};
```

### 2.3 クラス設計

#### 2.3.1 Ballクラス
```javascript
class Ball {
    constructor(x, y, vx, vy) {
        this.position = { x, y };
        this.velocity = { vx, vy };
        this.radius = 8;
        this.trail = [];  // 残像効果用
    }
    
    update() { /* 位置更新、衝突判定 */ }
    draw() { /* ボール描画、残像効果 */ }
    checkWallCollision() { /* 壁衝突処理 */ }
    checkPaddleCollision(paddle) { /* パドル衝突処理 */ }
    checkBlockCollision(blocks) { /* ブロック衝突処理 */ }
}
```

#### 2.3.2 Paddleクラス
```javascript
class Paddle {
    constructor(x, y) {
        this.position = { x, y };
        this.width = gameConfig.paddle.width;
        this.height = gameConfig.paddle.height;
        this.isExpanded = false;
        this.isSlowed = false;
        this.expandTimer = 0;
        this.slowTimer = 0;
    }
    
    update() { /* タイマー更新、効果時間管理 */ }
    draw() { /* パドル描画 */ }
    expandPaddle() { /* 拡大効果適用 */ }
    slowPaddle() { /* 速度低下効果適用 */ }
}
```

#### 2.3.3 Blockクラス
```javascript
class Block {
    constructor(x, y, color, isSpecial, itemType) {
        this.position = { x, y };
        this.width = blockLayout.width;
        this.height = blockLayout.height;
        this.color = color;
        this.isSpecial = isSpecial;
        this.itemType = itemType;
        this.isDestroyed = false;
    }
    
    draw() { /* ブロック描画、アイテム模様表示 */ }
    destroy() { /* 破壊エフェクト、アイテム生成 */ }
}
```

#### 2.3.4 Itemクラス
```javascript
class Item {
    constructor(x, y, type) {
        this.position = { x, y };
        this.type = type;
        this.velocity = { vx: 0, vy: 2 };
        this.animationFrame = 0;
        this.image = itemConfig.types[type].image;
    }
    
    update() { /* 落下処理、アニメーション */ }
    draw() { /* アイテム描画 */ }
    checkPaddleCollision(paddle) { /* パドル衝突判定 */ }
}
```

### 2.4 音響システム設計

#### 2.4.1 音響ファイル管理
```javascript
const audioSystem = {
    sounds: {
        click: 'mp3/click.mp3',
        destroy: 'mp3/hakai.mp3',
        get: 'mp3/get.mp3',
        reflect: 'mp3/reflect.mp3'
    },
    
    playSound(soundName) { /* 効果音再生 */ },
    toggleMute() { /* ミュート切り替え */ }
};
```

### 2.5 UI設計

#### 2.5.1 HUDレイアウト
```
┌─────────────────────────────────┐
│ Score: 0000        Lives: ♥♥♥  │
│                    Level: 1    │
│                    [PAUSE]     │
├─────────────────────────────────┤
│                                 │
│        ゲーム画面               │
│                                 │
└─────────────────────────────────┘
```

#### 2.5.2 画面別UI設計

**オープニング画面**
```javascript
function drawOpening() {
    // タイトル表示
    // "CLICK TO START"メッセージ
    // ハイスコア表示
}
```

**ゲームプレイ画面**
```javascript
function drawGame() {
    drawBackground();
    drawBlocks();
    drawBalls();
    drawPaddle();
    drawItems();
    drawHUD();
    drawParticles();  // 爆発エフェクト
}
```

**ゲームオーバー画面**
```javascript
function drawGameOver() {
    // "GAME OVER"表示
    // 最終スコア表示
    // ハイスコア更新通知
    // "CLICK TO RESTART"メッセージ
}
```

### 2.6 入力システム設計

#### 2.6.1 PC入力処理
```javascript
function mouseMoved() {
    if (gameState === GAME_STATE.PLAYING) {
        paddle.position.x = constrain(mouseX, paddleWidth/2, width - paddleWidth/2);
    }
}
```

#### 2.6.2 モバイル入力処理
```javascript
function touchMoved() {
    if (gameState === GAME_STATE.PLAYING) {
        paddle.position.x = constrain(touches[0].x, paddleWidth/2, width - paddleWidth/2);
    }
    return false;  // デフォルト動作防止
}
```

### 2.7 レスポンシブ設計

#### 2.7.1 画面サイズ適応
```javascript
function calculateScaleFactor() {
    const scaleX = windowWidth / gameConfig.canvas.width;
    const scaleY = windowHeight / gameConfig.canvas.height;
    return Math.min(scaleX, scaleY, 1.0);
}

function windowResized() {
    gameConfig.canvas.scaleFactor = calculateScaleFactor();
    resizeCanvas(
        gameConfig.canvas.width * gameConfig.canvas.scaleFactor,
        gameConfig.canvas.height * gameConfig.canvas.scaleFactor
    );
}
```

## 3. 実装フロー

### 3.1 メイン処理フロー
```javascript
function draw() {
    switch(gameState) {
        case GAME_STATE.OPENING:
            updateOpening();
            drawOpening();
            break;
        case GAME_STATE.PLAYING:
            updateGame();
            drawGame();
            break;
        case GAME_STATE.PAUSED:
            drawGame();
            drawPauseOverlay();
            break;
        case GAME_STATE.GAME_OVER:
            updateGameOver();
            drawGameOver();
            break;
    }
}
```

### 3.2 ゲームロジック更新フロー
```javascript
function updateGame() {
    updatePaddle();
    updateBalls();
    updateItems();
    updateParticles();
    checkCollisions();
    checkLevelClear();
    checkGameOver();
}
```

### 3.3 衝突判定フロー
```javascript
function checkCollisions() {
    balls.forEach(ball => {
        // 壁衝突
        ball.checkWallCollision();
        
        // パドル衝突
        if (ball.checkPaddleCollision(paddle)) {
            audioSystem.playSound('reflect');
        }
        
        // ブロック衝突
        blocks.forEach(block => {
            if (!block.isDestroyed && ball.checkBlockCollision(block)) {
                block.destroy();
                audioSystem.playSound('destroy');
                createParticles(block.position.x, block.position.y);
                
                if (block.isSpecial) {
                    spawnItem(block.position.x, block.position.y, block.itemType);
                }
            }
        });
    });
}
```

## 4. パフォーマンス考慮事項

### 4.1 最適化ポイント
- **描画最適化**: 不要な再描画を避ける
- **メモリ管理**: 不要なオブジェクトの適切な削除
- **音響最適化**: 音声ファイルのプリロード
- **衝突判定最適化**: 範囲チェックによる早期リターン

### 4.2 制限事項
- 同時ボール数上限: 10個
- パーティクル数上限: 50個
- 音声同時再生数: 制限なし（ブラウザ依存）

## 5. テスト設計

### 5.1 テスト項目
- [ ] ゲーム状態遷移テスト
- [ ] 衝突判定精度テスト
- [ ] アイテム効果テスト
- [ ] レスポンシブ動作テスト
- [ ] 音響システムテスト
- [ ] データ保存/復元テスト

### 5.2 デバッグ機能
```javascript
const DEBUG_MODE = false;

function drawDebugInfo() {
    if (DEBUG_MODE) {
        // 衝突判定範囲表示
        // FPS表示
        // ゲーム変数表示
    }
}
```

## 6. デプロイメント

### 6.1 分割後のファイル構造
```
block_break/
├── index.html          # エントリーポイント（正しい読み込み順序）
├── sketch.js           # p5.jsメイン関数のみ（約100行）
├── style.css           # UI スタイル
├── config.js           # ⚙️ 設定ファイル：ゲーム設定、定数、レイアウト設定
├── js/
│   ├── classes/        # 🏗️ クラス定義
│   │   ├── Ball.js     # ボールクラス（移動、描画、衝突判定）
│   │   ├── Paddle.js   # パドルクラス（入力処理、効果管理）
│   │   ├── Block.js    # ブロククラス（描画、破壊アニメーション）
│   │   ├── Item.js     # アイテムクラス（落下、効果適用）
│   │   └── Particle.js # パーティクルクラス（エフェクト）
│   ├── systems/        # 🔧 システム管理
│   │   ├── gameState.js # ゲーム状態管理、スコアシステム
│   │   ├── input.js    # 入力システム（マウス・タッチ統合）
│   │   ├── ui.js       # UI描画（オープニング、HUD、ポーズ）
│   │   └── effects.js  # エフェクトシステム（パーティクル管理）
│   └── utils/          # 🛠️ ユーティリティ
│       ├── collision.js # 衝突判定ロジック
│       └── gameLogic.js # ゲームロジック、ブロック生成
├── img/                # 画像リソース
│   ├── hp.png
│   ├── shield.png
│   ├── ball.png
│   └── skull.png
└── mp3/                # 音声リソース
    ├── click.mp3
    ├── hakai.mp3
    ├── get.mp3
    └── reflect.mp3
```

#### 6.1.1 モジュール分割の利点
- **保守性向上**: 機能別に分離され、バグ修正や機能追加が容易
- **可読性向上**: 各ファイルが100-200行程度で見通しが良い
- **再利用性**: クラスとユーティリティが独立して再利用可能
- **協調開発**: チーム開発時に競合が起きにくい構造

### 6.2 ブラウザ対応
- Chrome 60+
- Firefox 55+
- Safari 11+
- Edge 79+

この設計書に基づいて実装を進めることで、要望書で定義された機能を満たすブロック崩しゲームを効率的に開発できます。
